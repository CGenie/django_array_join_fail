# django_array_join_fail
Example of failing JOIN query with Django's Postgres ArrayField


To run:

```
./manage.py migrate
DJANGO_SETTINGS_MODULE=django_array_join_fail.settings python failing.py
```
